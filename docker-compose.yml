version: "3.8"

services:
    # Разработка
    app-dev:
        build:
            context: .
            target: development
        ports:
            - "5173:5173"
        volumes:
            - .:/app
            - /app/node_modules
        environment:
            - NODE_ENV=development
        profiles:
            - dev

    # Продакшен
    app-prod:
        build:
            context: .
            target: production
        ports:
            - "8080:80"
        environment:
            - NODE_ENV=production
        profiles:
            - prod

    # Сборка
    app-build:
        build:
            context: .
            target: build
        volumes:
            - ./dist:/app/dist
        profiles:
            - build
